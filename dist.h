// Copyright 2019-2023 Kai D. Gonzalez

#include "grid.h"

// some crazy wizard tricks to calculate the distance 
// between 2 coordinates and set it to m
void dist(coordinates * a, coordinates *b, coordinates * m);

// this function will return the distance of both points if both points are the
// same
int dists(coordinates * a, coordinates * b);
